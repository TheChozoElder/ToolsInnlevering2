<Window x:Class="innlevering2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.ignore.com"
        mc:Ignorable="d ignore"
        Height="500"
        Width="700"
        Title="MVVM Light Application"
        DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="View/Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="LayoutRoot">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <TabControl Margin="10,0,10,0" x:Name="MyTabControl">
            
            <TabItem Header="Enemies">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Enemy stats will be here" />
                   
                </Grid>
            </TabItem>
            
            <TabItem Header="Player">
                <Grid Margin="10" >
                    <TextBlock Text="Player stats / boss stats" />
                </Grid>
            </TabItem>
            
        </TabControl>

        <Button Content="Export to Json" Click="ExportButton" Grid.Row="1" Width="260" HorizontalAlignment="Left" Margin="10,0,0,10"/>
        <Button Content="Change filepath" Click="ExportButton" Grid.Row="1" Width="100" Margin="0,0,0,15" />
        <Button Content="Import from Json" Click="ImportButton" Grid.Row="1" Width="260" HorizontalAlignment="Right" Margin="0,0,10,10"/>
    </Grid>
</Window>
